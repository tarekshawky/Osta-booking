DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="osta-dev-secret-change-in-production-please"
NEXTAUTH_URL="http://localhost:3000"

# --- Email (optional) ---
# If SMTP_* values are not set, emails are logged to the server console instead of sent.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="OSTA Services <no-reply@osta.ae>"
# Admin address that receives a copy of every new appointment
ADMIN_EMAIL="admin@osta.ae"
